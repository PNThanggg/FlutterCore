name: app_core
description: "A new Flutter package project."
version: 0.0.1

environment:
  sdk: '>=3.4.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  logger:
    path: ../logger

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0